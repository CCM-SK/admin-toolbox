import { $, escapeHtml, downloadText } from '../utils.js';
export function renderDiff(app){app.innerHTML=`<section class="card"><h2>Configuration diff</h2><div class="drop-grid"><div><label for="a">Left / old</label><textarea id="a" placeholder="Paste old configuration"></textarea><input id="af" type="file" hidden></div><div><label for="b">Right / new</label><textarea id="b" placeholder="Paste new configuration"></textarea><input id="bf" type="file" hidden></div></div><div class="row" style="margin-top:10px"><button class="btn" id="ap">Load left file</button><button class="btn" id="bp">Load right file</button><button class="btn primary" id="go">Compare</button><button class="btn" id="exp">Export diff</button></div></section><section class="card"><div id="diffOut"></div></section>`;$('#ap').onclick=()=>$('#af').click();$('#bp').onclick=()=>$('#bf').click();$('#af').onchange=async e=>$('#a').value=await e.target.files[0].text();$('#bf').onchange=async e=>$('#b').value=await e.target.files[0].text();$('#go').onclick=compare;$('#exp').onclick=()=>downloadText('diff.txt',buildDiff($('#a').value,$('#b').value));function compare(){$('#diffOut').innerHTML=render(buildDiff($('#a').value,$('#b').value))}function buildDiff(a,b){const A=a.split(/\r?\n/),B=b.split(/\r?\n/);const N=Math.max(A.length,B.length),out=['--- old','+++ new'];for(let i=0;i<N;i++){if(A[i]===B[i])out.push('  '+(A[i]??''));else{if(A[i]!==undefined)out.push('- '+A[i]);if(B[i]!==undefined)out.push('+ '+B[i]);}}return out.join('\n')}function render(d){return `<div class="mono">${d.split('\n').map(l=>`<div class="${l.startsWith('+ ')?'diff-add':l.startsWith('- ')?'diff-del':'diff-same'}">${escapeHtml(l)}</div>`).join('')}</div>`}}
